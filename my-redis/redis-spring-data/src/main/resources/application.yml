spring:
  redis:
    host: localhost
    port: 6379
    database: 0
    lettuce:
      pool:
        max-active: 8 #連接池最大連接數，使用負值表示無限制，默認為 8
        max-wait: -1  #連接持最大阻塞時間，使用負值表示無限制，默認為 -1
        max-idle: 8   #連接池中最大的空閒連接，默認為 8
        min-idle: 0   #連接池中最小的空閒連接，默認為 0